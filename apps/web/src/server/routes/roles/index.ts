// Barrel for the role-plane route apps — keeps the historical
// `./routes/roles` import path working after the god-file split.
export { rolesRoutes } from "./roles";
export { permissionsRoutes } from "./permissions";
export { usersRoutes } from "./users";
