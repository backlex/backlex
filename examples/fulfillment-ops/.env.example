# Slug of the backlex workspace this console manages.
VITE_BACKLEX_WORKSPACE=sevkiyat
# Where the dev proxy forwards /api/* — your running `bun run dev` backend.
VITE_BACKLEX_PROXY_TARGET=http://localhost:5173

# ── Demo pre-fill (optional) ───────────────────────────────────────────────
# Set BOTH and the sign-in form opens already filled in, so trying this example
# costs one click instead of retyping credentials you set yourself. Either one
# alone does nothing. Leave them empty for any build that is not a demo: unset
# is the default, and an unset var puts no credential in the bundle at all.
#
# The product's own version of this is server-side (DEMO_MODE — see
# docs/demo-mode.md), because only a server knows whether an instance is a
# playground. An example has no server of its own, so it asks its .env instead.
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=
