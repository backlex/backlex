{
  "name": "@backlex-examples/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The pieces every backlex example needs and none of them should own: the env spec, the setup check, a sign-in form, and the one design kit they all render with.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./theme.css": "./src/theme.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "backlex": "workspace:*",
    "react": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.10",
    "typescript": "7.0.2",
    "vite": "8.2.1"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.3.0"
  }
}
