{
  "name": "@backlex/migrate",
  "version": "0.0.1",
  "private": true,
  "description": "External-DB migration toolkit: source introspection, type mapping, and migration-plan building. Dependency-free — callers inject a SQL executor (postgres.js in the CLI, pglite in tests).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "7.0.2"
  }
}
